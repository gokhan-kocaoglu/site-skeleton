# Faz 8.2 — Mühürleme Sprinti Kanıt Raporu

- Plan/brief tarihi: 2026-07-03 (`docs/source-briefs/faz-8-2-sealing-brief.md`,
  onaylı plan). **Uygulama koşusu: 2026-07-19** (dosya adı plan/manifest'e
  sadıktır; içerideki kanıtlar koşu günündendir).
- Kapsam: C1–C13 commit zinciri (aşağıda) + C14/C15 doküman kapanışları.
- Verdict sözlüğü: `.claude/rules/common/verdict-policy.md`.
- Blok 4 kararı: **SEÇENEK A — repo public**; ruleset enforcement Active
  (kullanıcı teyidi, 2026-07-19).

## CI Kanıtı (N4 kuralının ilk uygulaması)

- Run URL: <https://github.com/gokhan-kocaoglu/site-skeleton/actions/runs/29685746130>
- Run #28 · commit `0e9b80d` · genel sonuç **Success**
- Job'lar: `quality-gate-ubuntu` PASS · `api-verify-testcontainers` PASS ·
  `hooks-and-structure-windows` PASS · `gitleaks-full-history` PASS
  ("No leaks detected")

## Commit Zinciri (C1–C13)

| Commit | İçerik |
|---|---|
| `d2f1c68` | fix(governance): handoff hedefleri 9 geçerli ajana sabitlendi (+ `handoffTargets` kuralı + negatif senaryo) |
| `74d329b` | docs(governance): verdict disiplini (kural 6) + baseline review şerhi (orijinal korunur) |
| `d0a13ff` | docs(governance): kapsam dili tek konvansiyon satırına hizalandı |
| `6047e7e` | docs(memory): seal-commit konvansiyonu + validator hatırlatması + fixture (52→53) |
| `4f5cc08` | chore(repo): tsbuildinfo takipten çıkarıldı + `trackedForbidden` kuralı |
| `497a4a4` | fix(web): sitemap lastModified `getLastUpdated().iso`'ya bağlandı |
| `f778b1e` | fix(web): public yüzey sürümsüz + tamamen İngilizce (en-US locale dahil) |
| `a299e0e` | feat(web): favicon seti (icon.svg + apple-icon.tsx, token paleti + belgeli hex muafiyeti) |
| `0a60bb1` | fix(web): metadata.test type-safety + ADR-0012 şerhi |
| `efda55c` | feat(gates): audit INCONCLUSIVE run-gates özetinde görünür (exit-code protokolü) |
| `dc15c7c` | fix(templates): admin-bff düzeltmeleri + ACTIVATION kapısı |
| `0e9b80d` | docs: dış repo pointer'ı redakte edildi (public hazırlığı) |

Session-05 mührü konvansiyonun ilk uygulaması olarak atıldı; mühür commit'leri
konvansiyon gereği (brief 1.4) hiçbir kanıtta referanslanmaz.

## Doğrulama Bataryası (0e9b80d çalışma ağacı, 2026-07-19)

```text
pnpm gate                     → 7/7 PASS (All gates PASS; audit dahil)
hook harness                  → PASS — 162 assertions (53 fixtures + settings bindings)
                                 [C0 baseline: 159 assertions / 52 fixtures — ARTIŞ]
verify-structure-negative     → 4/4 senaryo PASS [C0 baseline: 1 senaryo]
                                 (yasak pattern · geçersiz HANDOFF · ACTIVATION'sız
                                  apps/admin-bff · işaretsiz checklist)
verify-structure              → PASS — 889 checks OK [C0 baseline: 862]
mvn verify (Testcontainers)   → gate-test içinde PASS (HealthEndpointIT 3/3, Docker
                                 postgres:16); CI'da api-verify-testcontainers PASS
```

### C11 exit-code protokolü kanıtı (bozuk registry ile)

```text
node scripts/quality/gate-audit.mjs   (lokal)  → exit 2, "INCONCLUSIVE"
node scripts/quality/gate-audit.mjs   (CI=true)→ exit 1, "CI treats this as FAIL"
node scripts/quality/run-gates.mjs    (lokal)  → tabloda "audit INCONCLUSIVE",
                                                 "PASS_WITH_WARNINGS — audit
                                                 inconclusive locally", exit 0
```

### C12 BFF runtime kanıtı (curl)

```text
POST /auth/logout            → HTTP/1.1 204 No Content, gövde YOK, cookie temizlendi
POST /auth/login  ('{bad')   → HTTP/1.1 400 Bad Request, {"error":"invalid JSON"}
```

## Koşu Notları

1. Docker Desktop daemon koşu başında kapalıydı (Testcontainers
   "NoClassDefFound/ExceptionInInitializer" ile HealthEndpointIT kırmızı);
   daemon başlatıldı, testler yeşillendi. Kod değişikliği yok — resertifikasyon
   koşusundaki olayın aynısı, altyapı olayıdır, bulgu değildir.
2. `templates/admin-bff/server.mjs` ilk yazımda 125 satıra çıktı (cap 120);
   yorumlar sıkıştırılarak 120'ye indirildi, davranış değişmedi.

## Bilinçli Kabul — yazar e-postası (Blok 4 karar a)

Git geçmişi yazar kimliği `gokhan.kocaoglu <gokhan.kocaoglu@hotmail.com>`
tüm geçmişte mevcuttur ve **bilinçli olarak kabul edilmiştir** (rewrite yok —
kanıt/hash zinciri korunur). İleriye dönük noreply adresi opsiyonel kullanıcı
adımıdır.

## Sonuç

Faz 8.2'nin C1–C13 kapsamındaki tüm maddeleri kanıtla kapandı; bulgu→commit
eşlemesi `docs/audits/2026-07-03-recertification.md` Faz 8.2 ekindedir.
Risk defterinin yeni durumu: **boş** (R1–R6 kapandı; Seçenek A gereği R8
açılmadı).
