{
  "hook": "task-card-validator.js",
  "name": "legacy tool_input.subject schema still validates (and blocks when broken)",
  "stdin": {
    "hook_event_name": "TaskCreated",
    "tool_name": "TaskCreate",
    "tool_input": {
      "subject": "Sipariş iptali",
      "description": "contract-impact: DELETE /api/orders/{id} eklenir\nrace-condition: N/A\nauth-boundary: sahiplik kontrolü zorunlu\nrollback-plani: soft-delete geri alınabilir"
    }
  },
  "expect": {
    "exitCode": 2,
    "noStdout": true,
    "stderrIncludes": "schema=legacy"
  }
}
