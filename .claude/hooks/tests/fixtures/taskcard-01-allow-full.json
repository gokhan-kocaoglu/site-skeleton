{
  "hook": "task-card-validator.js",
  "name": "complete card passes silently (official schema)",
  "stdin": {
    "session_id": "abc123",
    "cwd": ".",
    "permission_mode": "default",
    "hook_event_name": "TaskCreated",
    "tool_name": "TaskCreate",
    "tool_input": {
      "title": "Health endpoint dokümantasyonu",
      "description": "contract-impact: GET /api/health yanıt şeması değişmiyor\nrace-condition: N/A — tek yazarlı statik doküman işi\nauth-boundary: Yok, public endpoint\nrollback-plani: git revert yeterli",
      "status": "pending"
    }
  },
  "expect": {
    "exitCode": 0,
    "noStdout": true,
    "stderrIncludes": "schema=official"
  }
}
