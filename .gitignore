# Reference repos (cloned locally for study, never committed)
refs/

# Dependencies
node_modules/

# Build outputs
.next/
dist/
target/
.turbo/
coverage/
*.tsbuildinfo

# Graphify output (hook test fixture is the one committed exception)
graphify-out/
!.claude/hooks/tests/fixtures/graph-present/graphify-out/

# Session close flag (armed by /finish-session, consumed by the Stop hook)
.claude/hooks/.session-close-pending

# Environment / secrets
.env
.env.*
!.env.example

# Local machine settings
.claude/settings.local.json

# Logs / OS noise
*.log
.DS_Store
Thumbs.db
